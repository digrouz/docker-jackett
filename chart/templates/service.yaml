---
kind: Service
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-svc
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/component: jackett
spec:
  ports:
  - port: 9117
    name: webui
    protocol: TCP
  selector:
    app.kubernetes.io/component: jackett
